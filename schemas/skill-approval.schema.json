{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Skill Approval",
  "description": "Vendor approval for an Agent Skill in the AI Registry",
  "type": "object",
  "required": ["skillId", "date", "source", "installConfigs"],
  "additionalProperties": false,
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1,
      "description": "Skill identifier using reverse-domain notation (e.g., io.github.anthropics/code-review)"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "ISO date of approval (YYYY-MM-DD)"
    },
    "source": {
      "type": "object",
      "required": ["url"],
      "additionalProperties": false,
      "description": "Location of the skill folder in a git repository",
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Git repository URL containing the skill"
        },
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Path within the repository to the skill folder (must contain SKILL.md). Omit if the skill is at the repository root."
        }
      }
    },
    "installConfigs": {
      "type": "array",
      "minItems": 1,
      "description": "Tool-specific installation configurations",
      "items": {
        "type": "object",
        "required": ["tool"],
        "additionalProperties": false,
        "properties": {
          "tool": {
            "type": "string",
            "description": "Tool ID this config targets (must match a tool in the AI Registry)"
          },
          "installUrl": {
            "type": "string",
            "format": "uri",
            "description": "Deep-link URL for one-click install (tool-specific protocol)"
          }
        }
      }
    }
  }
}

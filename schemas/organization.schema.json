{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Organization",
  "description": "Vendor organization metadata for the AI Registry",
  "type": "object",
  "required": ["id", "name", "description", "website"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Unique identifier for the organization (lowercase, alphanumeric, hyphens)"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name of the organization"
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Short description of the organization"
    },
    "website": {
      "type": "string",
      "format": "uri",
      "description": "Organization website URL"
    },
    "color": {
      "type": "string",
      "pattern": "^#[0-9a-fA-F]{6}$",
      "description": "Brand color in hex format (e.g., #1a1f71)"
    },
    "inferred": {
      "type": "boolean",
      "description": "True if this organization was pre-seeded from an official public source rather than participating directly in the registry. The website marks inferred organizations with a distinct \"Inferred\" badge and dashed-border treatment. Defaults to false when omitted."
    },
    "tools": {
      "type": "array",
      "description": "Tools this organization provides. Omit if the organization does not provide tools.",
      "items": {
        "type": "object",
        "required": ["id", "name"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "description": "Unique identifier for the tool"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Display name of the tool"
          },
          "skillInstallUrlPrefix": {
            "type": "string",
            "description": "URL prefix for auto-generating skill install URLs. The artifact skillId is appended literally.",
            "examples": ["theia://install-skill?id="]
          },
          "mcpInstallUrlPrefix": {
            "type": "string",
            "description": "URL prefix for auto-generating MCP server install URLs. The artifact serverId is appended literally.",
            "examples": ["theia://install-mcp?id="]
          }
        }
      }
    },
    "trusts": {
      "type": "array",
      "description": "Other organizations whose approvals this organization delegates to, so their approved artifacts are treated as approved here too, without duplicate approval files. Single-hop only — trust is not transitive.",
      "items": {
        "type": "object",
        "required": ["org", "artifactTypes"],
        "additionalProperties": false,
        "properties": {
          "org": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "description": "ID of the trusted organization"
          },
          "artifactTypes": {
            "type": "object",
            "description": "Which artifact types this trust entry applies to. Only \"skills\" is supported today.",
            "additionalProperties": false,
            "properties": {
              "skills": {
                "type": "object",
                "additionalProperties": false,
                "description": "Trust the organization's skill approvals. Empty today — reserved for future per-type conditions."
              }
            }
          }
        }
      }
    }
  }
}
